/**
 * Re-exports the store diff helper.
 * Previously contained legacy SubItem diff logic; replaced by the new
 * SubscriptionStore.diff() from @bilibili-notify/subscription.
 */
export { diff } from "@bilibili-notify/subscription";
