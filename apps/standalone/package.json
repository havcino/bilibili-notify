{
	"name": "@bilibili-notify/standalone-root",
	"version": "0.0.0",
	"private": true,
	"description": "Root for the standalone Bilibili-Notify product (Hono server + React dashboard). pnpm sub-workspace; not part of the root yarn workspace.",
	"scripts": {
		"dev": "pnpm -r --parallel --stream dev",
		"dev:server": "pnpm --filter @bilibili-notify/standalone-server dev",
		"dev:web": "pnpm --filter @bilibili-notify/standalone-web dev",
		"build": "pnpm -r build",
		"typecheck": "pnpm -r typecheck",
		"start": "pnpm --filter @bilibili-notify/standalone-server start"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"packageManager": "pnpm@10.33.4"
}
