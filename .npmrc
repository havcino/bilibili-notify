# Match the previous yarn classic-style flat hoist, so koishi's plugin loader
# (which expects deps to live in a single hoisted node_modules tree) keeps
# working as before. pnpm's default symlink layout is fine for the dashboard
# end but the koishi end relies on hoist-anywhere semantics.
node-linker=hoisted
